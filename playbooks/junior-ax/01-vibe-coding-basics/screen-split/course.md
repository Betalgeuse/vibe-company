# Course: 화면 분할 환경 설정 방법

## 📖 표준 레이아웃

```
┌─────────────────────────────────────────────────┐
│   영역 1: Editor      │   영역 2: Runtime       │
│   코드 작성           │   실행 결과 확인         │
│   VS Code/Vim         │   브라우저/앱            │
│                       │                         │
├───────────────────────┴─────────────────────────┤
│   영역 3: Terminal                              │
│   Git, 명령어 실행, 로그 확인                    │
└─────────────────────────────────────────────────┘
```

## 🛠️ 도구별 설정 방법

### 방법 1: VS Code (가장 쉬움, 입문자 추천)

**1단계: 에디터 분할**
```
1. VS Code 실행
2. Cmd+\ (또는 View → Split Editor)
3. 왼쪽에 코드, 오른쪽에 테스트 파일
```

**2단계: 터미널 추가**
```
1. Ctrl+` (또는 Terminal → New Terminal)
2. 하단에 터미널 자동 생성
3. 터미널도 분할 가능: 터미널 우클릭 → Split Terminal
```

**3단계: 브라우저 배치**
```
1. Cmd+Tab으로 브라우저 선택
2. macOS: 창 분할 기능 사용
   - 전체화면 → Mission Control → 드래그
3. Windows: Win+← / Win+→ 
```

**키보드 단축키:**
```
Cmd+1, Cmd+2: 에디터 간 이동
Ctrl+`: 터미널 토글
Cmd+B: 사이드바 토글
Cmd+J: 패널 토글
```

---

### 방법 2: iTerm2 + Tmux (중급자)

**1단계: iTerm2 설치**
```bash
brew install --cask iterm2
```

**2단계: Tmux 설치**
```bash
brew install tmux
```

**3단계: Tmux 세션 시작**
```bash
# 새 세션 시작
tmux new -s vibe-coding

# 화면 분할
Ctrl+b "    # 수평 분할 (위/아래)
Ctrl+b %    # 수직 분할 (좌/우)

# 영역 간 이동
Ctrl+b ↑↓←→  # 방향키로 이동
```

**4단계: 세션 저장 (다음에도 사용)**
```bash
# 세션에서 나가기 (세션은 유지)
Ctrl+b d

# 다시 돌아오기
tmux attach -t vibe-coding
```

**추천 레이아웃 스크립트:**
```bash
#!/bin/bash
# ~/scripts/vibe-setup.sh

tmux new-session -d -s vibe -n main

# Pane 1: Editor (위쪽)
tmux send-keys -t vibe 'cd ~/projects/my-app && vim .' C-m

# Pane 2: Browser/Runtime (오른쪽)
tmux split-window -h -t vibe
tmux send-keys -t vibe 'cd ~/projects/my-app && npm run dev' C-m

# Pane 3: Terminal (아래쪽)
tmux split-window -v -t vibe.0
tmux send-keys -t vibe 'cd ~/projects/my-app && git status' C-m

# Attach
tmux attach -t vibe
```

**사용:**
```bash
chmod +x ~/scripts/vibe-setup.sh
~/scripts/vibe-setup.sh
```

---

### 방법 3: Neovim + Tmux (고급자, 최고 효율)

**1단계: Neovim 설치**
```bash
brew install neovim
```

**2단계: Neovim에서 분할**
```vim
:split    " 수평 분할
:vsplit   " 수직 분할

" 이동
Ctrl+w h/j/k/l  " 방향키로 이동
Ctrl+w w        " 순환 이동
```

**3단계: Tmux와 조합**
```
┌─────────────────────────────────┐
│   Tmux Pane 1    │ Tmux Pane 2  │
│   ┌────────────┐ │              │
│   │ Neovim     │ │   Terminal   │
│   │ :vsplit    │ │              │
│   └────────────┘ │              │
├──────────────────┴──────────────┤
│   Tmux Pane 3: Git/Logs         │
└─────────────────────────────────┘
```

---

## 🎯 실전 예시

### 웹 개발 레이아웃

```
┌─────────────────────────────────────┐
│ app/page.tsx     │ localhost:3000   │
│ (코드 수정)      │ (실시간 미리보기)  │
│                  │                  │
├──────────────────┴──────────────────┤
│ npm run dev (터미널)                │
│ git status                          │
└─────────────────────────────────────┘
```

### 백엔드 개발 레이아웃

```
┌─────────────────────────────────────┐
│ server.ts        │ Postman/curl     │
│ (API 코드)       │ (API 테스트)      │
│                  │                  │
├──────────────────┴──────────────────┤
│ node server.ts (터미널)             │
│ tail -f logs/app.log                │
└─────────────────────────────────────┘
```

### 풀스택 개발 레이아웃 (3분할)

```
┌─────────────────────────────────────┐
│ 백엔드 코드      │ 프론트 코드       │
│                  │                  │
├──────────────────┴──────────────────┤
│ 서버 로그        │ 브라우저 콘솔     │
└──────────────────┴──────────────────┘
```

---

## 💡 고급 팁

### 1. 화면 크기별 최적 레이아웃

**작은 화면 (13인치):**
```
2분할만 사용
├─ 왼쪽: 에디터
└─ 오른쪽: 터미널 (브라우저는 별도 전체화면)
```

**중간 화면 (15-16인치):**
```
3분할
├─ 왼쪽: 에디터
├─ 오른쪽: 브라우저
└─ 하단: 터미널
```

**큰 화면 (27인치+, 외장 모니터):**
```
4분할
├─ 좌상: 에디터 1
├─ 우상: 에디터 2 (테스트 파일)
├─ 좌하: 터미널
└─ 우하: 브라우저
```

### 2. 자동 시작 스크립트

**~/.zshrc 또는 ~/.bashrc에 추가:**
```bash
alias vibe='~/scripts/vibe-setup.sh'
```

**사용:**
```bash
vibe  # 한 단어로 환경 시작!
```

### 3. 프로젝트별 레이아웃 저장

**tmuxinator 사용:**
```bash
brew install tmuxinator

# 프로젝트 레이아웃 생성
tmuxinator new my-app
```

**~/.config/tmuxinator/my-app.yml:**
```yaml
name: my-app
root: ~/projects/my-app

windows:
  - editor:
      layout: main-vertical
      panes:
        - vim .
        - npm run dev
  - git:
      panes:
        - git status
```

**사용:**
```bash
tmuxinator start my-app
```

---

## 🔗 관련 자료

- [Git Worktree 설정](../git-worktree-setup/) - 브랜치 전환 0초
- [No Context Switching](../no-context-switching/) - 집중력 유지
- Tmux 공식 문서: https://github.com/tmux/tmux
- iTerm2: https://iterm2.com/
